# Copy this file to .env and fill in your Anthropic API key.
# The key is used by the frontend to call the Claude API directly.
# For production, move this to server.py as a proxy instead.
VITE_ANTHROPIC_API_KEY=sk-ant-...
