---
phase: 07-options-trading-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schemas.py
  - frontend/src/data/drillQuestions.js
  - tests/test_schemas.py
autonomous: true
requirements: []
tags:
  - options
  - drills
  - quiz-bank
must_haves:
  truths:
    - "Backend `VALID_DRILL_KEYS` includes `options_setups` so `/api/train/ai-drill` accepts it without 400"
    - "`DRILL_QUESTIONS.options_setups` exists with at least 20 four-option questions"
    - "`DRILL_META.options_setups` exists with `type: 'options_quiz'` and a `skill` field referencing a member of `VALID_SKILLS`"
    - "After `node scripts/shuffle-answers.js frontend/src/data/drillQuestions.js`, options_setups correct-answer positions are spread across 0..3 (not all 0)"
    - "Question distribution matches D-05 weighting: more bull_put than bear_call than short questions"
  artifacts:
    - path: "backend/schemas.py"
      provides: "VALID_DRILL_KEYS list including 'options_setups'"
      contains: "options_setups"
    - path: "frontend/src/data/drillQuestions.js"
      provides: "options_setups question bank + DRILL_META entry"
      min_lines: 650
      contains: "options_setups"
  key_links:
    - from: "frontend/src/data/drillQuestions.js"
      to: "backend/schemas.py VALID_DRILL_KEYS"
      via: "string key 'options_setups'"
      pattern: "options_setups"
    - from: "DRILL_META.options_setups.skill"
      to: "backend/schemas.py VALID_SKILLS"
      via: "skill string must be a member of VALID_SKILLS"
      pattern: "setup_selection"
---

<objective>
Ship the static `options_setups` drill bank so the existing Train flow picks it up automatically. This unlocks options-aware practice questions without changing any drill component — only data and the backend allowlist.

Purpose: Per CONTEXT D-03 and D-05, the user needs a credit-spread drill bank weighted toward bull put spreads (primary live strategy), then bear call spreads, then shorts. The existing `POST /api/train/ai-drill` endpoint already accepts a `skill` parameter and only needs the allowlist update.

Output: A new options drill key wired into the existing Train UI and the existing AI drill endpoint, with shuffled answer positions and a passing schema test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-options-trading-module/07-CONTEXT.md
@.planning/phases/07-options-trading-module/07-PATTERNS.md
@.planning/phases/07-options-trading-module/07-UI-SPEC.md
@CLAUDE.md
@backend/schemas.py
@frontend/src/data/drillQuestions.js
@scripts/shuffle-answers.js
@tests/test_question_bank_scripts.py

<interfaces>
<!-- Key contracts the executor must conform to. Extracted from codebase. -->

From backend/schemas.py (current state, lines 6-32):
- `VALID_SKILLS` includes: chart_reading, entry_timing, risk_sizing, setup_selection, trade_management, emotional_discipline, technical_indicators, market_internals, short_selling, gap_trading
- `VALID_DRILL_KEYS` current members listed at lines 19-32; "options_setups" MUST be appended

From frontend/src/data/drillQuestions.js:
- `DRILL_QUESTIONS` is exported object at line 1; new key appended before closing `}` near line 605
- Question shape: `{ q: string, options: string[4], correct: 0|1|2|3, explanation: string, difficulty?: string, tags?: string[], source?: string }`
- `DRILL_META` exported at line 607; entries follow `{ label: string, type: string ending in "_quiz", skill: string }`
- Existing meta example (line 619): `short_selling: {"label":"Short Selling","type":"short_quiz","skill":"short_selling"}`
- `chartKey` field is OPTIONAL — options drills do NOT use it (per D-03 / UI-SPEC line 200)

From backend/routers/train.py:
- `/api/train/ai-drill` validates `body.skill in VALID_DRILL_KEYS` (line 252) before processing — adding "options_setups" to `VALID_DRILL_KEYS` is sufficient to unlock the AI variant

From scripts/shuffle-answers.js:
- CLI signature: `node scripts/shuffle-answers.js <path-to-drillQuestions.js>`
- Operates in-place; cycles correct-answer positions 0,1,2,3 round-robin across questions whose `correct: 0` line is detected
- Pattern detected: `      options: [` (6 leading spaces) — questions MUST use this indentation for the script to pick them up
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `options_setups` to backend `VALID_DRILL_KEYS` with schema test</name>
  <files>backend/schemas.py, tests/test_schemas.py</files>
  <read_first>
    - backend/schemas.py (current `VALID_DRILL_KEYS` block at lines 19–32; do not touch `VALID_SKILLS` at lines 6–17 — per CONTEXT D-03, `options_setups` is a drill key only)
    - tests/test_schemas.py (existing test conventions and imports)
    - tests/test_question_bank_scripts.py lines 121–125 (analog assertion pattern using `VALID_DRILL_KEYS`)
    - .planning/phases/07-options-trading-module/07-CONTEXT.md (D-03 section confirms drill-key addition only)
  </read_first>
  <behavior>
    - Test 1: `"options_setups"` appears in `backend.schemas.VALID_DRILL_KEYS`
    - Test 2: `"options_setups"` is NOT in `backend.schemas.VALID_SKILLS` (verifying we did not over-add per D-03 scope)
  </behavior>
  <action>
    Per D-03: append the literal string `"options_setups"` as the last element of `VALID_DRILL_KEYS` in `backend/schemas.py` (the list currently ends with `"gap_trading",` at line 31). Do not modify `VALID_SKILLS`. Do not change ordering of existing entries. Write the new test cases in `tests/test_schemas.py` (create file with standard pytest module structure if it does not already cover this — match conventions in `tests/test_question_bank_scripts.py` lines 121–125). The test asserts membership in `VALID_DRILL_KEYS` and non-membership in `VALID_SKILLS`. No new imports beyond `from backend.schemas import VALID_DRILL_KEYS, VALID_SKILLS`.
  </action>
  <verify>
    <automated>pytest tests/test_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"options_setups\"" backend/schemas.py` returns at least 1
    - `python -c "from backend.schemas import VALID_DRILL_KEYS; assert 'options_setups' in VALID_DRILL_KEYS"` exits 0
    - `python -c "from backend.schemas import VALID_SKILLS; assert 'options_setups' not in VALID_SKILLS"` exits 0
    - `pytest tests/test_schemas.py -x -q` exits 0 with the two new assertions passing
  </acceptance_criteria>
  <done>
    Backend allowlist accepts `options_setups`; AI drill endpoint will no longer 400 for this skill. Schema test pins the contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `options_setups` question bank (20 questions) and `DRILL_META` entry, then shuffle answer positions</name>
  <files>frontend/src/data/drillQuestions.js</files>
  <read_first>
    - frontend/src/data/drillQuestions.js (full file — focus on the `short_selling` block at lines 521–562 as the exact-shape analog without `chartKey`, and the `DRILL_META` block at lines 607–621)
    - scripts/shuffle-answers.js (lines 1–50 of the executor — confirms the `      options: [` 6-space indent pattern and `      correct: 0,` detection so authored questions are eligible for shuffling)
    - .planning/phases/07-options-trading-module/07-CONTEXT.md (D-03 question topics, D-05 strategy priority, "Drill question distribution" in `<specifics>` block)
    - .planning/phases/07-options-trading-module/07-UI-SPEC.md (lines 184–211 — confirms `type: "options_quiz"`, `skill: "options_setups"` label `Options Spread Setups`)
  </read_first>
  <action>
    Author the `options_setups` drill bank inside `DRILL_QUESTIONS` and the matching `DRILL_META` entry, then shuffle. Concrete specifications:

    1. Append a new key `options_setups: [ ... ]` to the `DRILL_QUESTIONS` object before its closing brace at approximately line 605 (after the `gap_trading` block). Follow exact short_selling-block formatting (lines 521–562): each question on its own line, 6-space indented `options: [` array, `correct: 0,` initial value (the shuffler will rotate positions), and `explanation` + `difficulty` + `tags` + `source` fields filled out. Do NOT include `chartKey` — options drills are text-only per D-03.

    2. Write at minimum 20 questions in this distribution (per CONTEXT `<specifics>` and D-05):
       - 7 bull put spread questions (primary strategy — increase from baseline 5 to weight toward live strategy per D-05)
       - 6 bear call spread questions (second priority)
       - 4 strike-selection / probability questions (delta-as-probability, OTM distance, premium-vs-POP tradeoffs)
       - 3 behavioral / exit-discipline questions (close at 50% max profit, no plan before entry, oversizing on credit spreads)

    Cover the topic checklist verbatim from CONTEXT D-03: bull put when-to-use (high IV + bullish/neutral bias), max loss = width − premium, breakeven = short_strike − premium, close target = 50% max profit; bear call as mirror; strike selection via delta proxy; behavioral exit discipline. At least 2 of the bull put questions must reference the 50%-of-max-profit close rule explicitly. Difficulty mix: ≥ 6 beginner, ≥ 8 intermediate, ≥ 4 advanced. Set `source` to `"Phase 7 — Options Trading Module"` for all new questions.

    3. Add the `DRILL_META` entry after the existing `gap_trading` line (currently line 620), using the exact key/value `options_setups: {"label":"Options Spread Setups","type":"options_quiz","skill":"setup_selection"},` (insert immediately after the `gap_trading` line and before the closing brace of `DRILL_META`). The `skill` value is `"setup_selection"` (per PATTERNS section "DRILL_META — Note on `skill` field") because `VALID_SKILLS` has no dedicated `options` member. Do NOT add `options_setups` to `VALID_SKILLS` (that was rejected in D-03 — drill key only). The `type` value `"options_quiz"` follows the existing `_quiz` suffix convention.

    4. After saving the file, run `node scripts/shuffle-answers.js frontend/src/data/drillQuestions.js` from the project root. This rewrites `correct: 0,` lines to round-robin positions 0→1→2→3 only for questions that match the script's 6-space `options: [` regex — author all new questions with that exact indentation.
  </action>
  <verify>
    <automated>node -e "const d = require('./frontend/src/data/drillQuestions.js'); const q = d.DRILL_QUESTIONS.options_setups; if (!Array.isArray(q) || q.length < 20) throw new Error('expected >= 20 questions, got ' + (q && q.length)); for (const item of q) { if (!Array.isArray(item.options) || item.options.length !== 4) throw new Error('bad options shape'); if (![0,1,2,3].includes(item.correct)) throw new Error('bad correct index'); if (typeof item.q !== 'string' || !item.q.length) throw new Error('bad q'); if (typeof item.explanation !== 'string' || !item.explanation.length) throw new Error('bad explanation'); } const positions = q.map(i => i.correct); const uniquePositions = new Set(positions); if (uniquePositions.size < 3) throw new Error('correct positions not shuffled: ' + JSON.stringify([...uniquePositions])); if (!d.DRILL_META.options_setups || d.DRILL_META.options_setups.type !== 'options_quiz' || d.DRILL_META.options_setups.skill !== 'setup_selection') throw new Error('DRILL_META.options_setups misconfigured'); console.log('OK', q.length, 'questions');"</automated>
  </verify>
  <acceptance_criteria>
    - `DRILL_QUESTIONS.options_setups` is an array of length ≥ 20
    - Every question has exactly 4 `options` strings and a `correct` index in {0,1,2,3}
    - No question contains a `chartKey` field (text-only per D-03)
    - At least 3 distinct values appear in the set of `correct` indices across the 20 questions (shuffler ran successfully)
    - `DRILL_META.options_setups` deep-equals `{ label: "Options Spread Setups", type: "options_quiz", skill: "setup_selection" }`
    - Counting via grep: `grep -c '"difficulty":"beginner"' frontend/src/data/drillQuestions.js` increases by at least 6 vs. pre-task baseline; `grep -o '"setup":"Phase 7' | wc -l` is irrelevant — instead, all new questions have `"source":"Phase 7 — Options Trading Module"` so `grep -c "Phase 7 — Options Trading Module" frontend/src/data/drillQuestions.js` returns ≥ 20
    - Bull put weighting: `grep -ci "bull put" frontend/src/data/drillQuestions.js` exceeds `grep -ci "bear call" frontend/src/data/drillQuestions.js` (more bull put question mentions than bear call mentions in the new block, satisfying D-05)
  </acceptance_criteria>
  <done>
    Options Spread Setups drill renders automatically inside Train.jsx via the existing `availableDrills.map()` loop when the user's `active_skills` contains `setup_selection` (which is the canonical mapping from `DRILL_META.options_setups.skill`). Mastery bar wires up automatically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/train/ai-drill` | User-supplied `skill` value crosses into backend; existing `VALID_DRILL_KEYS` allowlist gates it |
| static data file → bundled SPA | `drillQuestions.js` is shipped to all users; question text and explanation strings render via React (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `VALID_DRILL_KEYS` allowlist | mitigate | Backend already validates `body.skill in VALID_DRILL_KEYS` at `backend/routers/train.py` line 252; adding `options_setups` does not weaken the gate. No new injection surface. |
| T-07-01-02 | Information Disclosure | `drillQuestions.js` content shipped in bundle | accept | Static educational content. No PII, no secrets. Authoring guideline: do not paste real trade-account numbers or API keys into question text or explanations. |
| T-07-01-03 | Tampering / Cross-Site Scripting | Question `q` and `explanation` rendered in QuizDrill | accept | React JSX auto-escapes text. All new questions are plain text — no `dangerouslySetInnerHTML`. Authoring guideline: do not include raw HTML in question content. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan. `node` and `scripts/shuffle-answers.js` are already in the repo; no `npm install` invoked. |
</threat_model>

<verification>
1. `pytest tests/test_schemas.py -x -q` passes the two new assertions
2. The Node verification block in Task 2's `<automated>` passes — confirms shape, length, shuffled positions, and DRILL_META entry
3. Manual smoke (not gating, but recommended after wave completion): start dev backend with `DEV_BYPASS_AUTH=true uvicorn backend.main:app --port 7432`, frontend with `npm run dev`, sign in, ensure user's `active_skills` includes `"setup_selection"`, navigate to Train — the "Options Spread Setups" row appears with mastery bar
</verification>

<success_criteria>
- Backend allowlist accepts `options_setups` (schema test green)
- ≥ 20 four-option text-only questions exist under `DRILL_QUESTIONS.options_setups`
- `DRILL_META.options_setups` configured with `type: "options_quiz"`, `skill: "setup_selection"`, `label: "Options Spread Setups"`
- Correct-answer positions distributed (shuffler ran successfully)
- Bull put questions outnumber bear call questions (D-05 weighting honored)
</success_criteria>

<output>
Create `.planning/phases/07-options-trading-module/07-01-SUMMARY.md` when done. Include: count of questions added, distribution by topic, final shuffled-position histogram, and confirmation that no `chartKey` was added.
</output>
